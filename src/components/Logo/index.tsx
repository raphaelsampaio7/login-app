import React from 'react';
import {Container, LogoText} from './styles';

const Logo: React.FC = () => {
  return (
    <Container>
      <LogoText>LoginApp</LogoText>
    </Container>
  );
};

export default Logo;
